% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitQuasar2CR.R
\name{estimateDispersionsCR}
\alias{estimateDispersionsCR}
\title{Estimate per-SNP dispersion psi = log(1/M) by Cox-Reid APL with
empirical-Bayes shrinkage to a smooth trend in log baseMean.}
\usage{
estimateDispersionsCR(
  dd,
  design = ~1,
  eps = 0.001,
  interval = c(-15, 15),
  psi_init = -log(20),
  verbose = TRUE
)
}
\arguments{
\item{dd}{data frame with columns \code{identifier}, \code{R}, \code{A}
and any variables in \code{design}.}

\item{design}{one-sided formula.  Default \code{~ 1}.}

\item{eps}{shared sequencing error (scalar; not estimated here).}

\item{interval}{Brent interval for psi (default \code{c(-15, 15)}).}

\item{psi_init}{initial psi for Brent.}

\item{verbose}{logical.}
}
\value{
data frame with one row per identifier: \code{baseMean},
  \code{psi_gw}, \code{psi_trend}, \code{var_samp}.  Trend, prior variance
  and eps are attached as attributes.
}
\description{
This is exposed separately from \code{\link{fitQuasar2CR}} so users can
inspect or override the dispersion step in isolation.
}
